install:
	@echo "🚀 Installing xpander-agno-agent..."
	pip install -r requirements.txt
	@echo "✅ Installation complete! xpander dev for event listener or invoke directly with xpander agent invoke \"AGENT_NAME\" \"Hello, what can you do?\""
